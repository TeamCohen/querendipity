/**
 * Tomcat servlet for autocomplete and GHIRL graph access; also provides access 
 * to metadata displayed in search results page taken from the <code>.all</code>
 * text files in data/ (not to be confused with the package {@link nies.data}, 
 * which manages the sleepycat DBs).
*/
package nies.metadata;